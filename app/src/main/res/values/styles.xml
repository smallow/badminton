<resources>



    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- translucent system bars -->
        <!--        <item name="android:windowTranslucentStatus">true</item>
                <item name="android:windowTranslucentNavigation">true</item>-->
        <!--选中状态icon的颜色和字体颜色-->
        <!--<item name="android:fitsSystemWindows">true</item>-->
        <item name="colorPrimary">@android:color/holo_red_light</item>
        <item name="colorPrimaryDark">@android:color/holo_red_light</item>
        <item name="colorAccent">@android:color/holo_green_light</item>
        <!--正常状态下字体颜色和icon颜色-->
        <!--<item name="android:textColorPrimary">@android:color/white</item>-->

    </style>

    <style name="MyActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
<!--
        <item name="android:textStyle">bold</item>
-->
        <item name="android:textColor">@android:color/black</item>

    </style>



    <style name="AppTheme.TransparentActivity" parent="AppBaseTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>


    <style name="custom_progress_dialog_one_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否悬浮在activity上   false: dialog 布满整个屏幕， true，显示指定大小-->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题   false 的时候，在没有给定title的情况下，会比true的情况显示要长（title显示了，没有内容而已），-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 背景颜色-->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 false： 整个界面不会有遮罩层-->
        <item name="android:backgroundDimAmount">0.6</item>
        <!-- 灰度  当android:backgroundDimEnabled=true时，这个参数才有用，值越大，遮罩层颜色越深。-->
        <item name="android:windowAnimationStyle">@style/custom_progress_dialog_one_animation</item>
        <!-- 弹出或者进入时的动画效果 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 对话框是否有遮盖 -->
        <item name="android:colorBackgroundCacheHint">@null</item>
        <!-- 背景缓存颜色 -->
    </style>


    <style name="custom_progress_dialog_two_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/custom_progress_dialog_two_bg</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>


    <style name="custom_progress_dialog_three_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->

    </style>


    <!-- 自定义进度对话框1动画-->
    <style name="custom_progress_dialog_one_animation">
        <item name="android:windowEnterAnimation">@anim/custom_progress_dialog_one_fading_in</item>
        <item name="android:windowExitAnimation">@anim/custom_progress_dialog_one_fading_out</item>i
    </style>


</resources>
